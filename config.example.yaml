# Config template — copy to config.yaml and fill in your values

target: local
db:
  host: localhost
  name: wlmonitor_dev
  user: wlmonitor
  password: your_password
deploy:
  web_root: /Library/WebServer/Documents
  dest: /Library/WebServer/Documents/wlmonitor
auth_db:
  host: localhost
  socket: /tmp/mysql.sock
  name: jardyx_auth
  user: wlmonitor
  password: your_password
slack:
  bot_token: your_bot_token
app:
  name: WL Monitor
  support_email: contact@eriks.cloud
  base_url: http://wlmonitor.test
APP_CODE: wlm
